{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Server posts",
  "main": "api/server.js",
  "scripts": {
    "start": "json-server -w db.json -p 3002"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
